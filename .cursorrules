# Regras Globais do Projeto Assusa

## 📝 Padrão de Commits (CRÍTICO)

**NUNCA acumule commits grandes. Sempre divida em fases e partes.**

- Cada commit deve representar uma **funcionalidade específica** e ser **compreensível por si só**
- Commits devem ser **pequenos e focados** para facilitar manutenção e revisão
- Respeite o padrão **Conventional Commits** já existente no projeto:
  - Formato: `tipo(escopo?): descrição em português`
  - Tipos: `feat`, `test`, `docs`, `chore`, `fix`, `refactor`
  - Escopo é **opcional** e deve refletir a camada/módulo afetado:
    - `adapters` - para adapters (whatsapp, sicoob, google, redis, http)
    - `domain` - para entidades, ports, use cases, value objects
    - `application` - para serviços de aplicação
    - `infra` - para infrastructure (config, logging, security)
    - `docker` - para Dockerfile e configurações de container
  - Descrição deve ser **específica e clara** sobre o que foi implementado/alterado

### Exemplos de Commits Válidos (baseados no histórico do projeto):
- ✅ `feat(adapters): implementa adapter WhatsApp Cloud API`
- ✅ `feat(adapters): implementa RateLimiter com Redis`
- ✅ `feat(domain): implementa use case GerarSegundaVia`
- ✅ `test: adiciona testes para CPF Handler`
- ✅ `docs: adiciona documentação completa do projeto`
- ✅ `chore: remove arquivos .gitkeep desnecessários`

### Exemplos de Commits INVÁLIDOS:
- ❌ `feat: implementa sistema completo` (muito genérico, várias funcionalidades)
- ❌ `fix: corrige bugs e adiciona features` (múltiplas mudanças)
- ❌ `chore: atualiza código` (pouco descritivo)

**Regra de Ouro**: Se você precisar usar "e" ou "também" na mensagem do commit, provavelmente precisa dividir em múltiplos commits.

### Configuração de Usuário Git

- ❌ **NUNCA alterar** o nome e email do commit da configuração global do Git
- ✅ **SEMPRE usar** a configuração global existente (`git config --global user.name` e `git config --global user.email`)
- Se for necessário usar configuração específica do repositório, documentar o motivo em `docs/adr/` ou comentário no `.cursorrules`

## 🌐 Idioma e Comunicação

- Sempre responder em **português**
- Documentação e comentários em código devem estar em **português**
- Mensagens de commit em **português**

## 🏗️ Arquitetura

O projeto segue **Clean Architecture (Ports & Adapters)**. Respeite a separação de camadas:

- **Domain** (`src/domain/`): Regras de negócio puras, sem dependências externas
  - Entities: Entidades de domínio
  - Use Cases: Casos de uso
  - Value Objects: Objetos de valor
  - **Apenas ports puramente de domínio** (genéricos e abstratos, raros)

- **Application** (`src/application/`): Serviços que orquestram casos de uso
  - Services: Serviços de aplicação
  - **Ports de integrações externas** (`src/application/ports/driven/`): Interfaces de integrações externas
    - WhatsApp, Sicoob, Google Drive, Google Sheets, Redis, HTTP, etc.
  - **Ports de entrada** (`src/application/ports/driving/`): Interfaces de controllers/handlers (se existirem)

- **Adapters** (`src/adapters/`): Implementações concretas das portas
  - http: Servidor Fastify
  - whatsapp: Adapter WhatsApp Cloud API
  - sicoob: Adapter Sicoob API
  - google: Adapters Google Drive/Sheets
  - redis: Adapter Redis (com fallback em memória)
  - in-memory: Implementações em memória para desenvolvimento/testes

- **Infrastructure** (`src/infrastructure/`): Configuração, logging, segurança

### 📍 Regra Arquitetural: Localização de Ports

**CRÍTICO**: Ports de integrações externas **NUNCA** devem ficar em `src/domain/ports/`.

- ✅ **Ports de integrações externas** → `src/application/ports/driven/`
  - Exemplos: `WhatsAppPort`, `SicoobPort`, `DrivePort`, `SheetsPort`, `StoragePort`, `RateLimiter`, `Logger`, etc.
  - **Critério**: Se o nome do port "parece integração", ele NÃO é domínio.

- ✅ **Ports puramente de domínio** (raros) → `src/domain/ports/`
  - Exemplos: `Clock`, `IdGenerator`, `Hasher`, `RandomProvider`, abstrações de políticas de domínio
  - **Critério**: Genéricos, abstratos, não são integrações específicas.

**Migração gradual**: Ports legados em `src/domain/ports/` possuem arquivos ponte (reexports) durante a migração incremental.

**Referência**: Ver ADR-0001 em `docs/adr/ADR-0001-ports-na-application.md`

## 💻 Padrões de Código TypeScript

- Use **TypeScript** com tipos explícitos (evitar `any`)
- Nomes de variáveis e funções em **camelCase**
- Nomes de classes e interfaces em **PascalCase**
- Use **async/await** ao invés de `Promise.then()`
- Sempre tratar erros adequadamente com **try/catch**
- Use **interfaces** para definir contratos (Ports)
- Prefira **injeção de dependências** via construtor

## 🔒 LGPD e Segurança

- **CPFs sempre devem ser hasheados** antes de armazenar (usar `CpfHandler.hashCpf()`)
- **CPFs nunca devem aparecer em logs** - sempre usar mascaramento (`CpfHandler.maskCpf()`)
- Validar todos os inputs do usuário
- Nunca commitar dados sensíveis ou secrets
- Usar variáveis de ambiente para configurações sensíveis

## 🧪 Testes

- Adicionar **testes unitários** para novas funcionalidades
- Testes devem ser isolados e independentes
- Usar mocks para dependências externas
- Manter cobertura de testes adequada
- Testes em português (describe/it em português)

## 📦 Estrutura de Arquivos

- Um arquivo por classe/função principal
- Agrupar arquivos relacionados em diretórios
- Usar exports nomeados ao invés de default exports
- Seguir a estrutura de pastas já estabelecida

## 🔧 Desenvolvimento

- Usar `npm run dev` para desenvolvimento com hot-reload
- Executar `npm test` antes de commitar
- Verificar lint com `npm run lint`
- Type-check com `npm run type-check`

## 📚 Documentação

- Atualizar README.md quando adicionar funcionalidades significativas
- Documentar funções complexas com JSDoc
- Manter comentários explicativos apenas quando necessário (código deve ser auto-explicativo)

## 🤖 Regras Operacionais do Agente (Cursor)

**OBJETIVO**: Aumentar a taxa de acerto do desenvolvimento através de milestones testáveis, validação contínua e entrega incremental.

### Trabalho por Milestones

- Trabalhar em **MILESTONES pequenas e testáveis**. Implementar apenas **1 milestone por vez**.
- Sempre perguntar/confirmar implicitamente via execução: "rodou localmente? passou testes? validou manualmente?" (sem avançar se não houver como validar).
- **Proibido avançar** para a próxima milestone sem que a milestone atual esteja:
  - ✅ build ok (`npm run build`)
  - ✅ testes ok (`npm test` ou `pnpm test`, conforme o repo)
  - ✅ e uma forma clara de validação manual (curl/postman/whatsapp webhook simulado)

### Entrega ao Final de Cada Alteração

Sempre entregar no final de cada alteração:

1. **Lista de arquivos criados/alterados**
2. **Comandos exatos para rodar**
3. **Como testar** (passo a passo)
4. **Critérios de pronto** (Definition of Done) da milestone
5. **Possíveis falhas e como diagnosticar** (logs onde olhar)

### Informações Externas e Segredos

- Se faltar informação externa (ex.: credenciais, URLs, tokens, IDs de API):
  - ✅ usar **placeholders e variáveis de ambiente**
  - ✅ documentar exatamente quais env vars são necessárias e exemplos
  - ❌ **NUNCA hardcode de segredos**

### Não Inventar Payloads

- ❌ **Não assumir** campos/formatos de terceiros sem evidência no código/docs existentes
- ✅ criar `types/interfaces` + validação runtime (ex.: zod) quando fizer sentido
- ✅ quando um payload for incerto, implementar de modo **tolerante** e registrar **logs seguros**

### Mudanças Incrementais

- Mudanças devem ser **mínimas e incrementais**:
  - ✅ preferir PRs/diffs pequenos
  - ❌ evitar refactors grandes junto com feature nova

### Logs e LGPD

- ❌ **nunca logar dados sensíveis**
- ✅ usar sempre utilitário de sanitização/masking/hashing definido no projeto

### Resolução de Conflitos

- Se houver conflito entre regra do `.cursorrules` e o código existente:
  - ❌ não "chutar" uma decisão
  - ✅ registrar a decisão numa nota curta (ex.: `docs/adr/`) ou comentário no `.cursorrules`
  - ✅ seguir o padrão já adotado no repositório até haver decisão explícita